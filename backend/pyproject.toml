[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "EliezerLopes1", email = "eli74350@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "channels>=4.3.2",
    "django>=6.1",
    "psycopg2-binary>=2.9.12",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
